{
  "name": "web",
  "version": "1.0.0",
  "description": "First go through the below items and follow their installation guides:",
  "homepage": "https://github.com/Communalists/web#readme",
  "bugs": {
    "url": "https://github.com/Communalists/web/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Communalists/web.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "scripts": {
    "prisma-pull": "prisma db pull",
    "generate": "prisma generate",
    "init": "npx prisma migrate dev --name init",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "npm --prefix ./frontend/ install && npm --prefix ./backend/ install"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "dotenv-cli": "^8.0.0"
  },
  "devDependencies": {
    "prisma": "^6.3.1"
  }
}
